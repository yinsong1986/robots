"""Integration tests for the Zenoh mesh.

These tests require ``eclipse-zenoh`` to be installed.  They are skipped
when zenoh is missing so the standard ``hatch run test-integ`` invocation
stays green on minimal environments.
"""
